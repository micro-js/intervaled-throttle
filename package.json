{
  "name": "@micro-js/throttle-intervaled",
  "description": "A throttle that only fires on an interval.",
  "repository": "git://github.com/micro-js/throttle-intervaled.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
